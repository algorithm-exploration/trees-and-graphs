#include <assert.h>
#include <stdio.h>

// Interview Cake C Unit Test Framework, based on CuTest
#include "Cake/UnitTest.h"

unsigned int findDuplicate(const unsigned int *intArray, size_t length)
{
    // find a number that appears more than once ... in O(n) time
    

    return 0;
}
